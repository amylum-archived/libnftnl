name 'libnftnl'
org 'amylum'

licenses 'COPYING'

deps(
  libmnl: '1.0.4-6',
  'linux-api-headers': '4.9-rc4-1'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

build do
  run './autogen.sh'
  configure
  make
  install
end

test do
  # TODO: add tests
end
