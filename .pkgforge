name 'libnftnl'
org 'amylum'

licenses 'COPYING'

deps(
  libmnl: '1.0.4-15',
  'linux-api-headers': '4.11-4'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

build do
  run './autogen.sh'
  configure
  make
  install
end

test do
  # TODO: add tests
end
